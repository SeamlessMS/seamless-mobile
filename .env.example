# Zoho Desk API Configuration
ZOHO_ORG_ID=your_org_id
ZOHO_CLIENT_ID=your_client_id
ZOHO_CLIENT_SECRET=your_client_secret
ZOHO_REFRESH_TOKEN=your_refresh_token
ZOHO_DEPARTMENT_ID=your_department_id

# Server Configuration
PORT=3000

# Note: Replace the values above with your actual Zoho API credentials
# To get these values:
# 1. Go to https://api-console.zoho.com/
# 2. Create a new client or use an existing one
# 3. Generate a refresh token with Desk.tickets.CREATE and Desk.contacts.CREATE scopes
# 4. Get your organization ID from Zoho Desk settings
# 5. Get your department ID from Zoho Desk settings 